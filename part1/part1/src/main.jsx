import ReactDOM from 'react-dom/client'

import App from './App'

// renders the app.jsx contents into the div-element defined in the index.html
// having the id root 
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
